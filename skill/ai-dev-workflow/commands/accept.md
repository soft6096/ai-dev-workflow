---
description: 收敛验收：对照技术方案核对代码，输出验收报告（第⑤步）。触发词：验收/收敛/核对报告/check。用法：/accept <功能名或技术方案路径>
---

# 验收报告（收敛核对）

全部测试变绿后，对照技术方案逐条核对实现，输出**验收报告**。

## 输入

- 第③步的技术方案 md
- 已实现的代码 + 测试结果

## 执行

0. **关键规范自动核对（兜底闸门，所有类型通用，最优先）**：运行 `commands/check-standards.md` 核对（12 项 HIGH + 代码规范组 C1-C6 + 注释组 N1-N4 + INFO + 场景化，每项带标准 grep/ast-grep 指令）——**先判模式**（标准模式读 2.1-项目约束、存量适配读 0.5 扫描+2.1-存量适配），**选型敏感项（#1 接口文档/#2 日志框架/#3 数据访问层/#10 返回体）按项目约束/老项目约定判定**，其余按规范；**实际执行检查命令并核对证据（文件:行号），禁止凭记忆**。6 项核心（判定标准见 `templates/5.1-编码指令.md`"关于关键规范核对"）：
   1. **OpenAPI/Swagger**：springdoc/knife4j 依赖已引？Controller 有 @Tag/@Operation？DTO/VO 有 @Schema？开关按环境配置？
   2. **Logback**：logback-spring.xml 已提供（控制台+滚动文件+环境级 level）？全类 @Slf4j？无 System.out？
   3. **SQL 全在 XML**：无注解 SQL（@Select 等）？手写 SQL 全在 resources/mapper/*.xml？
   4. **详细设计 SQL 注释**：技术方案《数据模型与 SQL》所有 SQL 带注释（-- 用途/归属/条件）？
   5. **DDL 字段注释**：建表语句每字段 COMMENT + 表级 COMMENT？
   6. **JSON 入参/出参产物**：每个 Controller 功能项的 `3.<功能项序号>.2-<功能名>-接口清单（前后端通用）.md` 已生成且与方案一致（URL/方法/JSON 入参/JSON 出参成功+失败）？
   - 其余 6 项 HIGH（事务 rollbackFor / SQL 注入 / UPDATE-DELETE 带 WHERE / 统一返回体 / 密码加密 / 分页上限）+ INFO + 场景化项见 `commands/check-standards.md`
   - 发现任何 ❌（HIGH/C/N/INFO，不分重要与否）→ 追加为任务（T0xx）→ 返回 /implement **（实现前一起向用户确认是否补齐）** → 再跑测试 → 重新验收；补齐后仍 ❌ → 标注"需人工核对"，禁止带 ❌ 进入后续核对

1. 按模板 `templates/5.3-验收报告.md` 生成核对报告，按类型逐条核对：

**Controller 核对**：

| 核对项 | 对照来源 | 常见漂移 |
|---|---|---|
| 接口路径/方法/入参出参 | 技术方案接口清单 | AI 改参数名、漏字段 |
| 校验规则 | 技术方案入参校验 | 校验缺失或过严 |
| 表名/字段/类型/索引 | 技术方案《数据模型与 SQL》章节 | AI 自创列名 |
| Service 映射 | 技术方案接口清单 | 调错 Service 方法 |
| 权限/事务 | 项目约束 | 漏 @RequiresPermissions |

**Listener 核对**：队列/路由键/ACK、幂等逻辑、死信/重试、消息体校验
**Job 核对**：cron/调度、防重入锁、批处理、失败告警
**无入口核对（如适用）**：确无入口形态（非漏写）、核心逻辑/业务规则与方案一致
**注释核对（所有类型必做）**：对照项目约束"注释规范"章节——所有类（DTO/VO/Config 等，仅示例非穷举）类注释齐全、所有变量/字段注释齐全、所有方法（含测试方法）注释齐全（@param/@return 有业务含义）、方法体 ≥2 步有编号步骤注释、无逐行翻译式注释、复杂逻辑有 // WHY:、注释与代码一致（防编造）
**公共组件与重复代码核对（所有类型必做）**：对照技术方案"公共组件识别"小节 + 项目约束"公共组件规范"章节——Phase 0.5 公共组件已实现、使用点已复用无自留副本、全模块扫描无 ≥2 处相同/相似方法体、编码期 `// TODO(收敛)` 重复标记已处理。发现重复代码 → 追加"抽公共组件 + 改使用点"任务 → 返回 /implement

2. **quickstart 调通（必做，缺证据 = 验收不通过）**：mock/切片测试不加载完整 Spring 上下文、不连真实库，**测试绿 ≠ 能启动**。按模板 `templates/5.3-验收报告.md` 的"quickstart 调通证据"章节执行：
   - 环境准备：建库建表（执行技术方案《数据模型与 SQL》章节 DDL，或累计的 schema.sql 初始化脚本）、中间件启动、配置就位
   - **老项目场景（存量适配模式）**：老项目已接入的中间件与基础设施（数据库/缓存/消息队列/定时调度/文件存储/搜索引擎等，完整清单以 `0.5-存量代码扫描.md` 中间件清单为准）**直接复用老项目已有账号配置**连环境，确保流程能过、quickstart 成功；**老项目没有这些账号信息 → 停下来向开发人员索取，禁止编造/凭印象填写**
   - **全新构建**：`mvn clean package`（禁止用历史 jar/class 产物，旧产物可能含过期配置——真实事故：源码配置已修但 jar 未重打，启动即炸 `Unsupported character encoding 'utf8mb4'`）
   - 真实启动：记录 `Started ... in x.xxx seconds` + Tomcat 端口日志
   - 按技术方案"验收场景"逐条 curl 实测（合法 + 非法 + 边界三态），记录请求/期望/实测/结果
   - 调通对数据有污染（改密/删数据）→ 验证后复位

3. 核对出差异 → 追加为新任务（T009…）→ 返回 /implement 继续 → 再跑测试

## 输出

`docs/<模块名>V<版本号>-<YYYYMMDDHHMMSS>/5.3-<功能名>-验收报告.md`

## 完成标准

- [ ] **关键规范自动核对 12 项 HIGH + C/N 组全 ✅**（6 项核心实际检查 + 其余 HIGH 与代码规范/注释组以 `5.2-<功能名>-规范核对报告.md` 落盘报告为准，非凭记忆）
- [ ] 全部核对项 ✅ 或差异已追加任务处理
- [ ] 公共组件核对通过（无重复代码残留）
- [ ] 验收报告无"未解决差异"
- [ ] 人工抽查核心逻辑
- [ ] **quickstart 调通证据齐全（必填）**：全新构建 + 真实启动日志 + 按验收场景逐条实测（三态全覆盖），证据落盘在验收报告中；仅测试绿不算通过
- [ ] 功能清单逐项打勾，确认覆盖
