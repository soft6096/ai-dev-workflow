# 存量代码扫描 - <项目名>

> 使用说明：这是第 ⓪ 步"存量代码扫描与约束适配"（老代码基础上迭代新需求必做）的产物模板。扫描**只读不改代码**，产出"老项目约定基线"，供 2.1 项目约束（存量适配模式）引用。
> 填写要点：
> - 每项结论**必须有证据**（文件路径/配置项），禁止凭印象
> - 扫描深度：小项目全量；大项目抽样 2-3 个核心模块 + grep 兜底（返回体/命名/中间件配置必须全局确认）
> - 优化决策必须由人明确选择（优化哪几项 / 跳过）
>
> 📌 **样式要求**：速览框 / 对齐表格统一按 `00-中间产物-MD样式规范.md`。

> [!IMPORTANT] 文档速览
>
> | 项目 | 内容 |
> | :--- | :--- |
> | **扫描项目** | permission（RuoYi-Vue 系） |
> | **技术栈** | Spring Boot 2.7.x + MyBatis-Plus + MySQL 8 + Redis |
> | **返回体** | `R<T>`（com.permission.common.web.R） |
> | **优化决策** | 跳过（新代码对齐老项目） |
> | **扫描时间** | 2026-08-26 |

## 项目信息

| 项目 | 内容 |
| :--- | :--- |
| 项目路径 | /Users/xxx/permission |
| git 仓库 | 是（main 分支） |
| 构建状态 | `mvn compile` 通过 |
| 技术栈 | Spring Boot 2.7.x / MyBatis-Plus 3.5.x / MySQL 8 / Redis |

## 老项目约定基线（2.1 存量适配模式的约束来源）

### 1. 包结构

| # | 约定 | 证据 |
| :---: | :--- | :--- |
| 1 | 业务代码放 `com.permission.<模块>` 包，分层 controller / service / mapper / entity / dto / vo | src/main/java/com/permission/ |
| 2 | 公共组件放 `com.permission.common.web` / `com.permission.common.util` | src/main/java/com/permission/common/ |

### 2. 返回体与公共组件

| # | 约定 | 证据 |
| :---: | :--- | :--- |
| 1 | 统一返回体 `R<T>`（com.permission.common.web.R，code/message/data），分页 `PageResult` | R.java / PageResult.java |
| 2 | 异常统一抛 `ServiceException`，全局异常处理器 GlobalExceptionHandler 转换 | GlobalExceptionHandler.java |
| 3 | 可复用组件：TreeNode / PageResult / 分布式锁工具 ... | com.permission.common.util/ |

### 3. 命名风格

| # | 约定 | 证据 |
| :---: | :--- | :--- |
| 1 | Java 类名大驼峰、变量/方法小驼峰；变量 2-3 词有实义（如 `existingUser`、`pageResult`），禁止单字母/无实义缩写 | 抽样 AuthServiceImpl / RoleServiceImpl |
| 2 | 数据库表 `sys_` 前缀（业务表除外）、字段下划线、逻辑删除 `del_flag` | sys_user 表 DDL |
| 3 | 时间字段 DATETIME、金额 DECIMAL(10,2) | sys_order 表 DDL |

### 4. 中间件清单（版本 / 账号 / 接入方式 / 使用规范）

| 中间件 | 版本 | 账号/配置 | 接入方式 | 使用规范 | 证据 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| MySQL | 8.0 | 库 `permission_db` / 账号 `app_permission` | HikariCP + MyBatis-Plus | Mapper 接口 + XML 混用 | application-dev.yml |
| Redis | 5.x | 无密码（内网） | RedisTemplate | 锁 key 前缀 `perm:` | 现有代码 |
| RabbitMQ | 3.x | 账号 `perm` | @RabbitListener 自动 ACK | 消费做幂等（唯一键） | 现有 Listener |
| Quartz | 2.3.x | — | @Scheduled / Quartz Job | 防重入用 Redis 锁 | 现有 Job |

### 5. 既有约定（AGENTS.md / README / 接口文档）

| # | 约定 | 证据 |
| :---: | :--- | :--- |
| 1 | 接口文档用 Apifox，不用 Swagger | README.md |
| 2 | 新增接口必须 @RequiresPermissions 权限注解 | 现有 Controller |

## 优化决策（人确认）

> [!IMPORTANT] 优化决策（二选一）
> - **选择优化**：勾选以下规范项，先按所选规范优化存量代码，完成后更新本文件再进入 1.1
> - **选择跳过**：不优化存量代码，2.1 走**存量适配模式**，新代码按本文件约定对齐老项目

- [ ] java-code-standards（AI 开发代码规范）—— 优化存量 Java 代码（命名/分层/公共组件）
- [ ] comment-standards（AI 开发注释规范）—— 补全/优化存量注释
- [ ] database-standards（AI 开发 SQL 规范）—— 优化存量表结构/SQL/索引
- [x] **跳过**（本次不优化存量代码，新需求代码对齐老项目）

> 决策记录：由 **<人>** 于 <日期> 选择：跳过。2.1 必须按存量适配模式生成约束。

## 完成标准自检

- [ ] 包结构/返回体/命名风格/中间件/既有约定五项全部扫描并有证据（文件路径）
- [ ] 返回体、命名、中间件配置已全局确认（grep 兜底，非抽样）
- [ ] 优化决策已由人明确选择（优化哪几项 / 跳过）
- [ ] 选择跳过 → 已注明"2.1 走存量适配模式"
- [ ] 文档开头有速览框；表格分隔行带对齐标记