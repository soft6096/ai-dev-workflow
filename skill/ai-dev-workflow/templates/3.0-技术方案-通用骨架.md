# 技术方案：通用骨架（所有类型共用）

> 使用说明：这是第 ③ 步"技术方案"的产物模板（通用骨架，所有类型共用一份）。
> **生成一份完整技术方案 = 本骨架（基本信息 + 通用章节）+ 命中类型的 3.x 模板（四段式内容）+ 示例。**
> 3.x 类型模板是"类型导航表"里某一行的展开实例，只含类型特有内容，不含通用章节。
> 无入口形态的功能：只写本骨架，跳过类型段（见"第零段 功能定性"）。
>
> 📌 **样式要求**：生成前先读 `templates/00-中间产物-MD样式规范.md`——速览框 / 对齐表格 / mermaid 配色 / callout 统一按该规范。

## 使用流程

1. 从功能清单读功能项的**类型列**（Controller / Listener / Job / 无入口）
2. **第零段 功能定性**：无入口形态 → 只写通用章节，结束
3. 有入口形态 → 按【类型导航表】取对应 3.x 模板，把其四段内容并入本骨架
4. 复合形态（如 HTTP 接口 + MQ 触发补偿）→ 主类型四段为主，次类型对应段以小标题追加
5. 对照 3.x 模板的"类型特有自检" + 本骨架"完成标准自检"逐条核对

> [!IMPORTANT] 文档速览
>
> | 项目 | 内容 |
> | :--- | :--- |
> | **功能项** | #N |
> | **模块类型** | Controller / Listener / Job / 无入口 |
> | **优先级** | P1 / P2 / P3 |
> | **依赖** | #M / — |
> | **入口形态** | 有（HTTP/MQ/定时）/ 无 |

## 基本信息

- 功能项：#N | 类型：Controller / Listener / Job / 无入口 | 优先级：Px | 依赖：—

## 第零段 功能定性（先判，再决定走不走类型段）

| 判定维度 | 结论 | 依据 |
| :--- | :--- | :--- |
| 入口形态 | **有**（HTTP / MQ / 定时） / **无**（配置 / 字典 / 纯工具） | 有 → 走类型段；无 → 只写通用章节 |
| 业务复杂度 | **简单**（纯 CRUD 转发 / 单一表） / **复杂**（多表写 / 状态流转 / 算法 / 并发） | 复杂 → 核心逻辑**必写业务规则小节** |
| 跨功能项规则归属 | 定义在**最先触碰它的功能项**方案 / 引用 #N | 其他功能项只引用 + 写本入口触发的分支 |

## 通用章节（所有类型必填，一份）

### 验收场景（测试用例蓝本）

> [!NOTE] 覆盖口径
> 测试用例直接从这里翻译（第 ④ 步契约测试）。必须覆盖**合法 + 非法 + 边界**三态，无"待定/视情况"。

| # | 类别 | 场景 | 请求/输入 | 期望 |
| :---: | :---: | :--- | :--- | :--- |
| 1 | 合法 |  |  |  |
| 2 | 非法 |  |  |  |
| 3 | 边界 |  |  |  |

### 公共组件识别（架构自检）

> [!TIP] 抽取判定
> 防重复代码。重复/可复用逻辑 ≥2 个使用点 → 必须抽公共组件，供第 ④ 步拆任务时排入 Phase 0.5。
> 无状态纯逻辑 → 工具类（`common/util/`）；有状态/模板方法 → 抽象基类（`common/base/`）。无重复 → 填"无"。

| # | 重复/可复用逻辑 | 使用点 | 抽取类型 | 设计（签名/继承关系/包） |
| :---: | :--- | :--- | :--- | :--- |
| 1 |  |  |  |  |

### 注释要求

- 类/接口 Javadoc：功能说明、入参、出参、异常
- 对照《项目约束》"注释规范"章节（全量注释：所有类/变量/方法注释 + 方法体步骤注释 + // WHY: 约定）

## 类型导航表（入口形态 → 3.x 模板）

| 类型 | 模板 | 四段差异（一句话） | 必配图 |
| :--- | :--- | :--- | :--- |
| Controller（同步 HTTP） | `templates/3.1-技术方案-Controller.md` | 接口清单 / 请求响应 JSON + 校验 / 转发编排 / 权限事务 | 时序图 |
| Listener（异步 MQ） | `templates/3.2-技术方案-Listener.md` | 监听配置 / 消息体 + 字段约束 / 消费流程 / 幂等死信 | 时序图 |
| Job（定时触发） | `templates/3.3-技术方案-Job.md` | 调度配置 / 无入参 / 执行流程 / 防重入批处理 | 流程图 + 时序图 |

## 四段式结构（3.x 模板的统一语法）

每份类型模板按四段组织，本骨架定义其含义：

| 段 | 内容 | 备注 |
| :---: | :--- | :--- |
| 一、入口定义 | 功能怎么被触发：接口清单 / 监听配置 / 调度配置 | 所有类型必有 |
| 二、数据契约 | 入参出参形态：JSON / 消息体 / 无 | Job 无入参，可跳过 |
| 三、核心逻辑 | 主流程：消费流程 / 执行流程 / 转发编排；**复杂业务含业务规则（状态流转/计算规则/并发处理）** | 简单转发可跳过；复杂业务必写规则 |
| 四、关注点 | 横切：权限 / 事务 / 幂等 / 防重入 / 批处理 / 告警 | 按类型必选 |

> [!WARNING] 图与步骤配套
> 核心逻辑段**必配 mermaid 时序图（或流程图）+ 处理步骤编号列表**——图给直觉，步骤给细节；配色用样式规范统一主题变量，禁止自创。

## 扩展规则（新增入口形态时）

1. 本导航表加一行：类型 + 模板路径 + 一句话差异
2. 新建对应 3.x 模板（按四段式组织 + 完整示例 + 类型特有自检）
3. 通用章节、四段式语法、五步流程不动

## 完成标准自检（通用部分）

- [ ] 第零段功能定性正确（无入口形态已注明，未误走类型段）
- [ ] 基本信息完整（功能项编号/类型/优先级/依赖）
- [ ] 文档开头有速览框（`> [!IMPORTANT] 文档速览`）
- [ ] 验收场景含合法 + 非法 + 边界（无"待定/视情况"）
- [ ] 公共组件识别完成（无重复已注明"无"；有则给出签名 + 包路径）
- [ ] 注释要求已写明
- [ ] 类型特有内容来自导航表对应 3.x 模板，四段齐全
- [ ] 核心逻辑含 mermaid 图 + 处理步骤编号（配色用统一主题变量）
- [ ] 表格分隔行带对齐标记；大章节间用 `---` 分隔
