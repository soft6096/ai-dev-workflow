# 功能清单总览 - <模块名>

> 使用说明：这是第 ① 步"需求分析"的产物模板。把模块需求文档拆成功能项，每项一行。
> 填写要点：
> - **先过滤非 Java 服务端需求**：前端页面/移动端/小程序/其他语言服务端等 → 不拆功能项，记入下方"非 Java 服务端需求（已过滤）"表，不进入本流程开发范围
> - **类型**：Controller（HTTP 接口）/ Listener（MQ 消费者）/ Job（定时任务）/ 无入口（配置/工具，仅写通用章节）/ 其他（须注明触发方式）
> - **优先级**：P1（必须）/ P2（应该）/ P3（可选）
> - **依赖**：填写依赖的功能项编号，无则写 —
> - **验收标准**：必须可验证——"XX 可调通/可生效"，禁止写"实现 XX 功能"
> 下方示例为**订单模块**，复制后替换成你的业务。
>
> 📌 **样式要求**：速览框 / 对齐表格统一按 `00-中间产物-MD样式规范.md`。

> [!IMPORTANT] 文档速览
>
> | 项目 | 内容 |
> | :--- | :--- |
> | **模块** | 订单模块 |
> | **版本号** | V1.2.0（取自需求文档） |
> | **功能项数** | 8（Controller 5 · Listener 1 · Job 2） |
> | **优先级分布** | P1×6 · P2×2 |
> | **已过滤非 Java 需求** | 2（前端页面 ×1 · 移动端 ×1） |
> | **需求文档** | docs/需求/订单模块需求文档.md |

## 模块信息

| 项目 | 内容 |
| :--- | :--- |
| 模块名称 | 订单模块 |
| 版本号 | V1.2.0（PRD/需求文档版本，docs 目录命名用：`docs/订单模块V1.2.0-<YYYYMMDDHHMMSS>/`） |
| 需求文档 | docs/需求/订单模块需求文档.md |
| 负责人 | 张三 |
| 日期 | 2026-08-07 |

## 非 Java 服务端需求（已过滤）

> 本流程只开发 **Java 服务端**需求（Controller/Listener/Job/无入口）。以下需求点不属于 Java 服务端，**不拆功能项**，仅记录备查（建议由对应团队承接；**前端需求可走 vue-ai-dev 流程**，如已安装）。

| # | 需求点 | 过滤原因 | 建议承接方 |
| :---: | :--- | :--- | :--- |
| F1 | 订单列表页面 + 交互 | 前端页面，非 Java 服务端 | 前端团队 |
| F2 | 移动端订单推送 | 移动端 App，非 Java 服务端 | App 团队 |
| F3 | 订单导出 Excel 模板样式 | 前端模板设计，非 Java 服务端 | 前端团队 |

> [!NOTE] 若过滤项被误过滤（需求点实际需要 Java 服务端支撑），回到上方功能清单补拆功能项。

## 功能清单

| # | 功能项 | 类型 | 优先级 | 依赖 | 验收标准 |
| :---: | :--- | :---: | :---: | :--- | :--- |
| 1 | 订单创建 | Controller | P1 | — | POST /order 可创建订单，返回订单号 |
| 2 | 订单查询（分页） | Controller | P1 | — | GET /order/list 分页返回，total 正确 |
| 3 | 订单详情 | Controller | P1 | 依赖 #1 | GET /order/{id} 返回完整订单信息 |
| 4 | 订单取消 | Controller | P1 | 依赖 #1 | PUT /order/cancel 后状态变 CANCELLED |
| 5 | 订单支付回调 | Listener | P1 | 依赖 #1 | 收到消息后状态变 PAID，重复消息幂等 |
| 6 | 订单超时关闭 | Job | P2 | 依赖 #1 | 超 30 分钟未支付自动 CLOSED |
| 7 | 订单导出 Excel | Controller | P2 | 依赖 #2 | GET /order/export 返回 xlsx 文件 |
| 8 | 订单统计报表 | Job | P3 | 依赖 #2 | 每日凌晨生成昨日订单统计 |

## 拆解说明（可选）

| # | 说明 |
| :---: | :--- |
| #5 | 支付回调是消息驱动，不是 HTTP 接口，故类型标 Listener |
| #6 | 超时关闭是后台任务，标 Job |
| #7 | 导出虽然也是 GET，但按"独立验收"拆成单独功能项 |
| #4/#1 | 都围绕订单实体，Controller 边界内聚为 OrderController（技术方案 #1/#4 合并写一份） |

## 完成标准自检

- [ ] 每个功能项有类型标签（Controller/Listener/Job/无入口/其他）
- [ ] 每个功能项有可验证的验收标准（不是"实现 XX 功能"）
- [ ] 粒度可独立开发、独立测试、独立交付
- [ ] 依赖关系已标注
- [ ] 非 Java 服务端需求已过滤并记录（过滤表每项有原因 + 建议承接方；无过滤项则注明"无"）
- [ ] 版本号已从需求文档/PRD 读取并记录；docs 版本化目录已创建（`<模块名>V<版本号>-<YYYYMMDDHHMMSS>`）
- [ ] 文档开头有速览框；表格分隔行带对齐标记
