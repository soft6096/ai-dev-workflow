# 接口清单（前后端通用）- <模块名>

> 使用说明：这是第 ③ 步技术方案完成后生成的**模块级聚合产物**（独立文档，不并入 3.0 骨架）。
> **生成时机**：同模块所有 Controller 类型功能项的技术方案（3.x）生成完毕后，把每个方案「一、入口定义 → 接口明细」中的 URL / 入参 / 出参**汇总到本清单**，一份文档覆盖整个模块。
> **用途**：前端联调 / Mock / 后端实现核对的前后端通用接口契约——前端拿它做页面联调，后端拿它核对实现，接口评审拿它逐条过。
> **数据来源**：各功能项技术方案（3.1 接口明细）——**内容必须与方案一致，禁止另起一套说法**；方案改 → 本清单同步改。
>
> 📌 **样式要求**：速览框 / 对齐表格 / callout 统一按 `00-中间产物-MD样式规范.md`。

> [!IMPORTANT] 文档速览
>
> | 项目 | 内容 |
> | :--- | :--- |
> | **模块名** | <模块名>（对应功能清单模块） |
> | **关联功能项** | #1 用户管理 / #3 角色管理 / … |
> | **接口总数** | N（新增 M 个 + 复用 K 个） |
> | **基准版本** | V<版本号> |
> | **维护约定** | 接口变更必须同步本清单 + 对应技术方案，禁止只改一边 |

## 一、接口汇总表（模块全量，一接口一行）

| # | 功能项 | 方法 | URL | 入参（类型/必填） | 出参（类型） | 错误码 | 说明 |
| :---: | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | #1 用户管理 | POST | `/api/v1/users` | UserCreateDTO（体） | `Response<UserVO>` | 400/409/500 | 创建用户 |
| 2 | #1 用户管理 | GET | `/api/v1/users/{userId}` | userId（路径，必填） | `Response<UserVO>` | 404/500 | 用户详情 |
| 3 | #1 用户管理 | GET | `/api/v1/users/list` | UserQueryDTO（查询串） | `Response<PageResult<UserVO>>` | 400/500 | 分页查询 |
| … | … | … | … | … | … | … | … |

> [!NOTE] 复用接口
> 复用其他模块/基础平台的接口（如登录、文件上传）→ 在汇总表标注"复用 + 来源"，不重复展开明细。

## 二、接口明细（每个 URL 独立小节：URL + 方法 + 入参 JSON + 出参 JSON 成功/失败）

> [!WARNING] 入参/出参 JSON 必给示例
> 每个接口必须有 **入参 JSON 示例** + **出参 JSON 示例（成功与失败各一个，失败示例与汇总表错误码一一对应）**——前端据此零沟通直接联调，禁止只给字段表不给 JSON 示例。

### #1 POST /api/v1/users — 创建用户（功能项 #1）

**入参 JSON**：

```json
{
  "username": "zhangsan",
  "nickname": "张三",
  "email": "zhangsan@example.com",
  "phone": "13800138000",
  "password": "abc123456",
  "deptId": 101,
  "roleIdList": [1, 2],
  "status": "0"
}
```

**出参 JSON（成功 / 失败）**：

```json
// 成功
{ "code": 200, "message": "操作成功", "data": { "userId": 10001, "username": "zhangsan" } }
// 失败（用户名已存在，409）
{ "code": 409, "message": "用户名已存在", "data": null }
// 失败（参数校验错误，400）
{ "code": 400, "message": "email 格式不正确", "data": null }
```

### #2 GET /api/v1/users/{userId} — 用户详情（功能项 #1）

**入参**：路径参数 `userId`（Long，必填），无请求体

**出参 JSON（成功 / 失败）**：

```json
// 成功
{ "code": 200, "message": "操作成功", "data": { "userId": 10001, "username": "zhangsan", "email": "zhangsan@example.com", "status": "0" } }
// 失败（用户不存在，404）
{ "code": 404, "message": "用户不存在", "data": null }
```

### #3 GET /api/v1/users/list — 分页查询用户（功能项 #1）

**入参**：QueryString `pageNum=1&pageSize=10&username=zhang&status=0`

**出参 JSON（成功 / 失败）**：

```json
// 成功（分页）
{ "code": 200, "message": "操作成功", "data": { "total": 1, "rows": [ { "userId": 10001, "username": "zhangsan", "status": "0" } ] } }
// 失败（分页参数非法，400）
{ "code": 400, "message": "pageSize 不能大于 100", "data": null }
```

## 三、字段口径（跨接口共享字段/枚举/字典，避免各接口各说一套）

| 字段 / 枚举 | 取值 | 口径说明 |
| :--- | :--- | :--- |
| status | "0" 正常 / "1" 停用 | 用户状态，全局一致 |
| Response.code | 200 成功；4xx 客户端；5xx 服务端 | 统一返回体 `Response<T>`（code/message/data） |
| … | … | … |

## 完成标准自检

- [ ] 汇总表覆盖模块全部 HTTP 接口（新增 + 复用标注），无遗漏
- [ ] 每个接口有独立小节：URL + 方法 + 入参 JSON 示例 + 出参 JSON 示例（成功/失败）
- [ ] 失败示例与汇总表错误码一一对应（列了几个错误码给几个示例）
- [ ] 接口内容与对应技术方案（3.1 接口明细）一致，无两套说法
- [ ] 共享字段/枚举口径已登记，无同字段不同含义
- [ ] 接口变更已同步本清单 + 技术方案（双向一致）
