# 澄清问题清单 - <模块名>

> 使用说明：这是 Clarify 环节的产物模板。对功能清单逐项盘问歧义/入口复杂度/边界/依赖后填写。
> 每个问题由 AI 给出**建议默认值**，人确认后状态改为"已确认"；全部确认后才能进入技术方案。
> 示例为**订单模块**，复制后替换成你的业务。

## 澄清信息

- 功能清单：docs/功能清单-订单模块.md
- 澄清日期：2026-08-07
- 澄清人（AI/人）：AI 盘问，人确认

## 问题清单

| # | 涉及功能项 | 类型 | 问题 | AI 建议默认值 | 状态 |
|---|-----------|------|------|--------------|------|
| 1 | #4 订单取消 | 歧义 | 已发货的订单能否取消？ | 不允许，返回 409 + 提示"已发货不可取消" | ✅ 已确认 |
| 2 | #4 订单取消 | 边界 | 重复取消请求如何处理？ | 幂等：已取消则直接返回成功 | ✅ 已确认 |
| 3 | #4 订单取消 | 边界 | 部分发货状态能否取消？ | 不允许，返回 409 | ✅ 已确认 |
| 4 | #1 订单创建 | 依赖 | 是否依赖库存服务？ | 是，调用 StockService 扣减库存 | ✅ 已确认 |
| 5 | #1 订单创建 | 边界 | 库存扣减失败的处理策略？ | 事务回滚，返回 400 + "库存不足" | ✅ 已确认 |
| 6 | #1 订单创建 | 歧义 | "金额"字段是否含运费/优惠？ | 含运费减优惠，公式写入技术方案 | 🔲 待确认 |
| 7 | #3 订单支付回调 | 边界 | 重复支付消息（同订单两次 PAID）？ | 幂等：唯一键 order_id+status 跳过 | 🔲 待确认 |
| 8 | #6 订单超时关闭 | 边界 | 超时与用户手动取消并发？ | 状态位防重入，先到先得 | 🔲 待确认 |

## 决策记录（可选）

- #6 采用 AI 默认值（金额=含运费减优惠），已记录到技术方案
- #1 依赖库存服务为人工确认补充

## 完成标准自检

- [ ] 每个功能项无未确认的歧义
- [ ] 每个功能项的边界用例已列出（合法/非法/边界）
- [ ] 依赖完整标注（功能间 + 外部系统）
- [ ] 问题清单状态列无"待确认"残留
- [ ] 功能清单已更新（边界/风险列 + 依赖完善）
